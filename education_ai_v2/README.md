# Education AI v2
# Trợ lý giáo dục thông minh với RAG

## Chạy ứng dụng
```bash
cd education_ai_v2
streamlit run app.py
```

## Features
- 📤 Upload tài liệu (PDF, TXT, MD, CSV)
- ⚡ Tự động xử lý và index
- 💬 Hỏi đáp dựa trên tài liệu (RAG)
- 🔄 Streaming response
- 📊 Hiển thị trạng thái xử lý real-time

## Kiến trúc
```
┌────────────────────────────────────────────────┐
│                   Streamlit UI                  │
├────────────────────────────────────────────────┤
│  Upload File → Save → Process →  Display Status│
├────────────────────────────────────────────────┤
│                   RAG Engine                    │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────┐ │
│  │ File Reader │→ │ Chunking    │→ │ Embed   │ │
│  │ (PDF/TXT)   │  │ (512 words) │  │ (BGE-M3)│ │
│  └─────────────┘  └─────────────┘  └─────────┘ │
│                         ↓                       │
│              ┌─────────────────────┐            │
│              │   FAISS Vector DB   │            │
│              └─────────────────────┘            │
├────────────────────────────────────────────────┤
│                 Query Pipeline                  │
│  Query → Embed → FAISS Search → Rerank → LLM  │
│         (BGE-M3)  (Top 20)   (BGE-Reranker)   │
│                                    ↓            │
│                          Llama 3.1 8B (Stream) │
└────────────────────────────────────────────────┘
```

## Cấu trúc thư mục
```
education_ai_v2/
├── app.py          # Streamlit UI chính
├── rag_engine.py   # RAG Engine (xử lý file, vector store)
├── engine_llm.py   # LLM Engine (Llama 3.1 8B)
├── uploads/        # Thư mục lưu files upload
└── rag_data/       # Database (FAISS index, chunks)
```

## Models sử dụng
| Component | Model | Size |
|-----------|-------|------|
| Embedding | BAAI/bge-m3 | ~2GB |
| Reranker | BAAI/bge-reranker-v2-m3 | ~500MB |
| LLM | Llama 3.1 8B (4-bit) | ~5GB |

## Yêu cầu VRAM
~10-12GB VRAM để chạy tất cả models
